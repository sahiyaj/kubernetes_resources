apiVersion: v1
kind: Pod
metadata:
  name: config-pod
spec:
  containers:
  - name: nginx
    image: nginx
    env:
    - name: course
      valueFrom:
       configMapKeyRef:
        name: course-config
        key: course
    - name: learning
      valueFrom:
       configMapKeyRef:
        name: course-config
        key: duration
      
